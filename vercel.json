{
  "version": 2,
  "framework": "vite",
  "buildCommand": "cd client && npm run build",
  "installCommand": "cd client && rm -f package-lock.json && npm install",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
